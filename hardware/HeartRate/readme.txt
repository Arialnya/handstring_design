IR、lastbeat:用于检测返回的光强数据
其中，IR高速实时更新，低于5000时视为手指未接入
lastbeat值用于计算BPM（心率），显示时仅作为数值参考，实际使用时可以删除。
BPM（心率）
AVG BPM（平均心率）

由于稳定性较为良好，暂未加入滤波，如果遇到不稳定的情况，可以额外添加滤波。

其中，D5是复位接口，超过五秒钟持续保持一个数据（且确认已经接入手指时），给D5一个高电平，这样可以重置传感器，避免卡死的情况。

如果还是遇到数据持续未更新（或者乱码），可以断开VCC和GND再插一次，基本就好了