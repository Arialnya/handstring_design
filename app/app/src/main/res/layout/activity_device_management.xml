<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnConnect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:text="连接设备"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDeviceManagement"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:text="设备管理"
        app:layout_constraintTop_toBottomOf="@id/btnConnect" />

    <EditText
        android:id="@+id/bluetoothNameInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:hint="输入蓝牙名称"
        app:layout_constraintTop_toBottomOf="@id/btnDeviceManagement" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSetName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:text="设置蓝牙名称"
        app:layout_constraintTop_toBottomOf="@id/bluetoothNameInput" />

</androidx.constraintlayout.widget.ConstraintLayout> 